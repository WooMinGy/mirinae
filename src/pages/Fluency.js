import React from "react";
import Header from "../components/Header";

const Fluency = () => {
  return (
    <React.Fragment>
      <Header fluency={"fluency"} />
      <p>no data</p>
    </React.Fragment>
  );
};

export default Fluency;
